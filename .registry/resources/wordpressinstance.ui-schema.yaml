version: 0.1.0
configSections:
- title: Provision Details
  items:
  - name: region
    controlType: singleInput
    path: .spec.region
    title: Resource Region
    validation:
    - required: true
  - name: bucket
    controlType: singleInput
    path: .spec.bucket
    title: Bucket Name
    validation:
    - required: true
initialConfig: true